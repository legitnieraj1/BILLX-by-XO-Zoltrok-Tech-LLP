{
  "name": "billx-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "billx-pos"
  ],
  "scripts": {
    "build": "npm run build --prefix billx-pos",
    "install": "npm install --prefix billx-pos"
  }
}
